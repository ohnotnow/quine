<?php

/*
 * A fixture test the tia fixture does NOT name, so the overlay must report it
 * as missing from the coverage cache.
 */
it('renders the comments', function () {
    expect(true)->toBeTrue();
});
